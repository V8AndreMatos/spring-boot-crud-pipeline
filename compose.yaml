

services:
  mysql:
    image: mysql:latest
    container_name: mysql-db
    restart: always
    environment:
      - MYSQL_DATABASE=spring_pipeline_db
      - MYSQL_USER=root
      - MYSQL_PASSWORD=dodgedart79
      - MYSQL_ROOT_PASSWORD=dodgedart79
    ports:
      - '3306:3306'
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
